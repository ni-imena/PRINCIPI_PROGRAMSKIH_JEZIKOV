package data

fun main(args: Array<String>) {
    if (accessToken == "") {
        GetAccessToken()
    }
    /*
        val activities = GetActivityIds()
        for (act in activities){
            GetActivityList(act)
        }

        for (act in ActivityList){
            println(act)}
        */
}


